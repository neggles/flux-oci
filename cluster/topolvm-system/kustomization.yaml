---
apiVersion: kustomize.config.k8s.io/v1beta2
kind: Kustomization
resources:
  - helmrelease-topolvm.yaml
configMapGenerator:
  - name: topolvm-helm-chart-values
    namespace: topolvm-system
    files:
      - values.yaml=topolvm-helm-chart-values.yaml
configurations:
  - kustomizeconfig.yaml
